feat: Initialize server with user authentication and community features

- Add package.json for server dependencies and scripts
- Implement authentication routes for user registration, login, and profile retrieval
- Create community routes for managing posts, replies, and likes
- Develop event routes for public and business-specific event management
- Add user routes for upgrading accounts and updating profiles
- Implement integration tests for authentication and community features
- Create centralized API service functions for community and events
- Establish a color palette and theme configurations for consistent UI design
